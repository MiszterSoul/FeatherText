## Summary

Describe the problem and the focused solution.

## Changes

- Describe the implementation changes.

## Validation

List the commands and manual checks you ran.

- [ ] `npm run lint`
- [ ] `npm run test`
- [ ] `npm run test:e2e` (when behavior changes)
- [ ] `npm run test:a11y` (when UI or interaction changes)
- [ ] `npm run build`
- [ ] `npm run size` (when shipped assets change)
- [ ] `npm run package:check` (when package contents change)

## Visual changes

Add before/after screenshots or recordings when applicable.

## Release impact

- [ ] The PR title uses a Conventional Commit prefix such as `fix:`, `feat:`, or `docs:`.
- [ ] Breaking changes are marked with `!` and explained below.
- [ ] Documentation and examples are updated when needed.

### Breaking changes or migration notes

None.
